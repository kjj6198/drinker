var editableRow = document.querySelector('tr[data-editable="true"]');

function makeEditable(row) {
  row = Array.from(row);
  row = row.slice(1);
  row.forEach()
}

makeEditable(editableRow.children);
