.row.justify-content-md-center
  .col.col-md-6
    = simple_form_for(@menu) do |f|
      = f.error_notification

      .form-inputs
        = f.input :name
        = f.input :end_time
        - if action_name != 'edit'
          = f.input :channel, input_html: { placeholder: "which channel do you want to notify? e.g: #dev-frontend" }
        = f.input :drink_shop_id, collection: DrinkShop.all
        = f.input :max_count, label: "最大杯數（超過後截止訂單）"

      .form-actions
        = f.button :submit
